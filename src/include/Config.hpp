// --- Config.hpp ---
#pragma once

namespace Config {
    inline constexpr int WindowWidth = 800;
    inline constexpr int WindowHeight = 600;
    
    // You can also add other type-safe constants here!
    inline constexpr float DefaultCameraSpeed = 5.0f;
}
